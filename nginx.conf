events {
    worker_connections 1024;
}

http {
    upstream api {
        server api01:8080;
        server api02:8081;
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
